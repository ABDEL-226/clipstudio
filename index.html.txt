<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ClipStudio — Créer une vidéo IA</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #ff006e;
            --secondary: #8338ec;
            --accent: #3a86ff;
            --success: #10b981;
            --warning: #f59e0b;
            --danger: #ef4444;
            --bg: #050510;
            --bg-elevated: rgba(255,255,255,0.03);
            --bg-card: rgba(255,255,255,0.05);
            --border: rgba(255,255,255,0.06);
            --border-hover: rgba(255,255,255,0.12);
            --text: #ffffff;
            --text-muted: rgba(255,255,255,0.45);
            --text-dim: rgba(255,255,255,0.55);
            --spacing-xs: 8px;
            --spacing-sm: 12px;
            --spacing-md: 16px;
            --spacing-lg: 24px;
            --spacing-xl: 32px;
            --spacing-2xl: 48px;
            --radius-sm: 8px;
            --radius-md: 12px;
            --radius-lg: 16px;
            --radius-xl: 24px;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }
        html { scroll-behavior: smooth; }
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: var(--bg);
            color: var(--text);
            min-height: 100vh;
            overflow-x: hidden;
            line-height: 1.6;
        }

        /* ===== BACKGROUND EFFECTS ===== */
        .mesh-bg {
            position: fixed;
            inset: 0;
            z-index: 0;
            background:
                radial-gradient(ellipse 80% 50% at 20% 40%, rgba(255,0,110,0.12) 0%, transparent 60%),
                radial-gradient(ellipse 60% 40% at 80% 20%, rgba(131,56,236,0.10) 0%, transparent 50%),
                radial-gradient(ellipse 50% 60% at 50% 80%, rgba(58,134,255,0.08) 0%, transparent 50%),
                radial-gradient(ellipse 100% 100% at 50% 0%, rgba(255,0,110,0.03) 0%, transparent 50%),
                linear-gradient(180deg, #050510 0%, #0a0a1e 40%, #050510 100%);
            animation: meshPulse 12s ease-in-out infinite;
        }
        @keyframes meshPulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.85; }
        }

        .stars {
            position: fixed;
            inset: 0;
            z-index: 1;
            pointer-events: none;
            background-image:
                radial-gradient(1.5px 1.5px at 20px 30px, rgba(255,255,255,0.3), transparent),
                radial-gradient(1px 1px at 50px 100px, rgba(255,255,255,0.2), transparent);
            background-size: 1000px 500px;
            animation: twinkle 8s ease-in-out infinite alternate;
        }
        @keyframes twinkle {
            0% { opacity: 0.5; }
            100% { opacity: 1; }
        }

        .particles {
            position: fixed;
            inset: 0;
            z-index: 2;
            pointer-events: none;
            overflow: hidden;
        }
        .particle {
            position: absolute;
            border-radius: 50%;
            filter: blur(1px);
            animation: particleFloat linear infinite;
        }
        @keyframes particleFloat {
            0% { transform: translateY(110vh) translateX(0) scale(0); opacity: 0; }
            5% { opacity: 0.8; }
            95% { opacity: 0.8; }
            100% { transform: translateY(-10vh) translateX(40px) scale(1); opacity: 0; }
        }

        /* ===== LOGIN SCREEN ===== */
        .login-screen {
            position: fixed;
            inset: 0;
            z-index: 50;
            background: rgba(5,5,16,0.95);
            backdrop-filter: blur(24px);
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            transition: all 0.5s ease;
            opacity: 1;
            pointer-events: auto;
        }

        .login-screen.hidden {
            opacity: 0;
            pointer-events: none;
        }

        .login-card {
            width: 100%;
            max-width: 450px;
            animation: cardIn 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
        }

        @keyframes cardIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .login-header {
            text-align: center;
            margin-bottom: 40px;
        }

        .login-logo-wrap {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 80px;
            height: 80px;
            border-radius: 24px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            box-shadow: 0 12px 48px rgba(255,0,110,0.3);
            margin-bottom: 20px;
            position: relative;
            overflow: hidden;
        }

        .login-logo-wrap::after {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(135deg, transparent 40%, rgba(255,255,255,0.2) 50%, transparent 60%);
            animation: shimmer 3s infinite;
        }

        @keyframes shimmer {
            0% { transform: translateX(-100%); }
            100% { transform: translateX(100%); }
        }

        .login-logo-wrap svg { position: relative; z-index: 1; }

        .login-header h1 {
            font-size: 48px;
            font-weight: 800;
            margin-bottom: 8px;
        }

        .login-header p {
            font-size: 16px;
            color: var(--text-muted);
        }

        .login-form {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .form-label {
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            color: var(--text-muted);
        }

        .input-wrap {
            position: relative;
        }

        .input-icon {
            position: absolute;
            left: 16px;
            top: 50%;
            transform: translateY(-50%);
            width: 20px;
            height: 20px;
            color: var(--text-dim);
            transition: color 0.3s ease;
            pointer-events: none;
        }

        .form-input {
            width: 100%;
            padding: 14px 14px 14px 48px;
            font-size: 15px;
            font-family: inherit;
            color: var(--text);
            background: rgba(255,255,255,0.03);
            border: 1px solid var(--border);
            border-radius: 12px;
            outline: none;
            transition: all 0.3s ease;
        }

        .form-input::placeholder { color: var(--text-dim); }

        .form-input:focus {
            background: rgba(255,255,255,0.06);
            border-color: rgba(255,0,110,0.4);
            box-shadow: 0 0 0 3px rgba(255,0,110,0.08);
        }

        .login-forgot {
            text-align: right;
        }

        .login-forgot a {
            color: var(--primary);
            text-decoration: none;
            font-size: 13px;
            font-weight: 500;
        }

        .login-forgot a:hover {
            color: #ff4d9e;
        }

        .login-submit {
            width: 100%;
            padding: 14px;
            font-size: 15px;
            font-weight: 600;
            color: white;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border: none;
            border-radius: 12px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 24px rgba(255,0,110,0.25);
        }

        .login-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 32px rgba(255,0,110,0.35);
        }

        .login-submit:disabled {
            opacity: 0.6;
            cursor: not-allowed;
        }

        .login-footer {
            text-align: center;
            margin-top: 24px;
            font-size: 14px;
            color: var(--text-muted);
        }

        .login-footer a {
            color: var(--primary);
            text-decoration: none;
            font-weight: 600;
        }

        .login-footer a:hover {
            color: #ff4d9e;
        }

        .google-sign-in-btn {
            width: 100%;
            padding: 16px;
            font-size: 16px;
            font-weight: 700;
            color: white;
            background: linear-gradient(135deg, #4285F4, #34A853);
            border: none;
            border-radius: 12px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 24px rgba(66, 133, 244, 0.3);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        .google-sign-in-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 32px rgba(66, 133, 244, 0.4);
        }

        .google-sign-in-btn:active {
            transform: translateY(0) scale(0.98);
        }

        .back-btn {
            background: none;
            border: none;
            color: var(--primary);
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            padding: 8px 0;
            margin-bottom: 16px;
            transition: color 0.2s;
        }

        .back-btn:hover {
            color: #ff4d9e;
        }

        /* ===== NAVBAR ===== */
        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 100;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: var(--spacing-md) 5vw;
            background: rgba(5,5,16,0.7);
            backdrop-filter: blur(24px) saturate(180%);
            border-bottom: 1px solid var(--border);
            transition: all 0.3s ease;
            gap: var(--spacing-lg);
        }

        .nav-brand {
            display: flex;
            align-items: center;
            gap: var(--spacing-sm);
            font-weight: 800;
            font-size: 20px;
            letter-spacing: -0.5px;
            color: var(--text);
            text-decoration: none;
            flex-shrink: 0;
        }

        .nav-logo {
            width: 38px;
            height: 38px;
            border-radius: var(--radius-md);
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 4px 20px rgba(255,0,110,0.3);
            position: relative;
            overflow: hidden;
        }

        .nav-logo::after {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(135deg, transparent 40%, rgba(255,255,255,0.25) 50%, transparent 60%);
            animation: logoShine 4s infinite;
        }

        @keyframes logoShine {
            0% { transform: translateX(-100%); }
            100% { transform: translateX(100%); }
        }

        .nav-center {
            display: flex;
            align-items: center;
            gap: 8px;
            flex: 1;
            justify-content: center;
        }

        .nav-links {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .nav-link {
            padding: 8px 16px;
            font-size: 14px;
            font-weight: 500;
            color: var(--text-muted);
            text-decoration: none;
            border-radius: var(--radius-md);
            transition: all 0.2s ease;
            cursor: pointer;
        }

        .nav-link:hover, .nav-link.active {
            color: var(--text);
            background: rgba(255,255,255,0.06);
        }

        .nav-right {
            display: flex;
            align-items: center;
            gap: var(--spacing-sm);
            flex-shrink: 0;
        }

        .nav-credits {
            display: flex;
            align-items: center;
            gap: 6px;
            padding: 8px 16px;
            background: rgba(255,255,255,0.04);
            border: 1px solid var(--border);
            border-radius: var(--radius-md);
            font-size: 13px;
            font-weight: 600;
        }

        .nav-avatar {
            width: 36px;
            height: 36px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--secondary), var(--accent));
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 13px;
            cursor: pointer;
            transition: transform 0.2s;
        }

        .nav-avatar:hover { transform: scale(1.1); }

        /* ===== HERO ===== */
        .hero {
            position: relative;
            z-index: 10;
            padding: 140px 5vw 80px;
            text-align: center;
            max-width: 900px;
            margin: 0 auto;
        }

        .hero-badge {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 8px 18px;
            font-size: 13px;
            font-weight: 600;
            color: var(--primary);
            background: rgba(255,0,110,0.08);
            border: 1px solid rgba(255,0,110,0.15);
            border-radius: 100px;
            margin-bottom: 28px;
            animation: badgeIn 0.6s ease-out 0.2s both;
        }

        .hero-badge .pulse {
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background: var(--primary);
            box-shadow: 0 0 10px var(--primary);
            animation: pulse 2s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% { opacity: 1; transform: scale(1); }
            50% { opacity: 0.5; transform: scale(0.8); }
        }

        @keyframes badgeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .hero h1 {
            font-size: clamp(36px, 5.5vw, 72px);
            font-weight: 900;
            line-height: 1.1;
            letter-spacing: -2px;
            margin-bottom: 20px;
            animation: titleIn 0.8s ease-out 0.3s both;
        }

        .hero h1 .gradient-text {
            background: linear-gradient(135deg, var(--primary), var(--secondary), var(--accent));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            background-size: 200% 200%;
            animation: gradientShift 4s ease infinite;
        }

        @keyframes gradientShift {
            0%, 100% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
        }

        @keyframes titleIn {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .hero-subtitle {
            font-size: clamp(16px, 1.8vw, 22px);
            color: var(--text-muted);
            line-height: 1.6;
            max-width: 600px;
            margin: 0 auto 20px;
            animation: fadeIn 0.8s ease-out 0.5s both;
        }

        .hero-social {
            font-size: 13px;
            color: var(--text-dim);
            margin-bottom: 40px;
            animation: fadeIn 0.8s ease-out 0.6s both;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* ===== VIDEO GENERATOR ===== */
        .generator {
            position: relative;
            z-index: 10;
            padding: 0 5vw var(--spacing-2xl);
            max-width: 900px;
            margin: 0 auto;
            animation: fadeIn 0.8s ease-out 0.7s both;
        }

        .generator-card {
            background: var(--bg-elevated);
            backdrop-filter: blur(40px);
            border: 1px solid var(--border);
            border-radius: var(--radius-xl);
            padding: var(--spacing-xl);
            box-shadow: 0 25px 80px -20px rgba(0,0,0,0.6), 0 0 0 1px rgba(255,255,255,0.02) inset;
            transition: border-color 0.3s, box-shadow 0.3s;
        }

        .generator-card:focus-within {
            border-color: rgba(255,0,110,0.3);
            box-shadow: 0 25px 80px -20px rgba(0,0,0,0.6), 0 0 0 1px rgba(255,0,110,0.1) inset, 0 0 60px rgba(255,0,110,0.06);
        }

        .generator-section {
            margin-bottom: var(--spacing-lg);
        }

        .generator-section:last-child {
            margin-bottom: 0;
        }

        .generator-label {
            display: block;
            font-size: 13px;
            font-weight: 600;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: var(--spacing-sm);
        }

        .prompt-textarea {
            width: 100%;
            min-height: 120px;
            padding: var(--spacing-md);
            font-size: 17px;
            font-family: inherit;
            color: var(--text);
            background: rgba(255,255,255,0.02);
            border: 1px solid var(--border);
            border-radius: var(--radius-md);
            outline: none;
            resize: vertical;
            line-height: 1.6;
            transition: border-color 0.3s, background 0.3s;
        }

        .prompt-textarea:focus {
            border-color: rgba(255,0,110,0.3);
            background: rgba(255,255,255,0.04);
        }

        .prompt-textarea::placeholder {
            color: var(--text-dim);
        }

        .prompt-tools {
            display: flex;
            gap: var(--spacing-sm);
            margin-top: var(--spacing-sm);
            flex-wrap: wrap;
        }

        .btn-small {
            padding: 8px 14px;
            font-size: 12px;
            font-weight: 500;
            background: rgba(255,255,255,0.04);
            border: 1px solid var(--border);
            color: var(--text-muted);
            border-radius: var(--radius-sm);
            cursor: pointer;
            transition: all 0.2s;
        }

        .btn-small:hover {
            background: rgba(255,0,110,0.1);
            border-color: rgba(255,0,110,0.25);
            color: var(--text);
        }

        /* Reference Image Upload */
        .reference-upload {
            border: 2px dashed var(--border);
            border-radius: var(--radius-lg);
            padding: var(--spacing-lg);
            text-align: center;
            cursor: pointer;
            transition: all 0.3s;
        }

        .reference-upload:hover {
            border-color: rgba(255,0,110,0.3);
            background: rgba(255,0,110,0.02);
        }

        .reference-upload.has-image {
            border-style: solid;
            background: rgba(255,0,110,0.08);
        }

        .upload-placeholder {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: var(--spacing-sm);
        }

        .upload-icon {
            font-size: 32px;
            opacity: 0.6;
        }

        .upload-text {
            font-size: 14px;
            color: var(--text-muted);
        }

        .upload-hint {
            font-size: 12px;
            color: var(--text-dim);
            margin-top: var(--spacing-sm);
        }

        .reference-preview {
            width: 100%;
            height: 200px;
            border-radius: var(--radius-md);
            background: rgba(255,255,255,0.05);
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }

        .reference-preview img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .reference-remove {
            position: absolute;
            top: var(--spacing-sm);
            right: var(--spacing-sm);
            width: 32px;
            height: 32px;
            background: rgba(0,0,0,0.6);
            border: none;
            border-radius: 50%;
            color: white;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s;
        }

        .reference-remove:hover {
            background: rgba(239, 68, 68, 0.8);
        }

        /* Settings Grid */
        .settings-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: var(--spacing-md);
            margin-top: var(--spacing-md);
        }

        .setting-group {
            display: flex;
            flex-direction: column;
            gap: var(--spacing-sm);
        }

        .setting-pills {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(80px, 1fr));
            gap: var(--spacing-sm);
        }

        .pill {
            padding: 10px 14px;
            font-size: 13px;
            font-weight: 500;
            background: rgba(255,255,255,0.04);
            border: 1px solid var(--border);
            border-radius: var(--radius-md);
            cursor: pointer;
            transition: all 0.2s;
            text-align: center;
        }

        .pill:hover {
            background: rgba(255,255,255,0.06);
            border-color: var(--border-hover);
        }

        .pill.active {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border-color: transparent;
            color: white;
            box-shadow: 0 4px 16px rgba(255,0,110,0.25);
        }

        .advanced-toggle {
            display: flex;
            align-items: center;
            gap: var(--spacing-sm);
            padding: var(--spacing-md) 0;
            cursor: pointer;
            color: var(--text-muted);
            font-size: 13px;
            font-weight: 500;
            border: none;
            background: none;
        }

        .advanced-toggle:hover {
            color: var(--text);
        }

        .chevron {
            transition: transform 0.3s;
        }

        .advanced-toggle.open .chevron {
            transform: rotate(180deg);
        }

        .advanced-settings {
            display: none;
            padding-top: var(--spacing-md);
            border-top: 1px solid var(--border);
            gap: var(--spacing-md);
        }

        .advanced-settings.open {
            display: flex;
            flex-direction: column;
        }

        /* Enhance Prompt Panel */
        .enhance-prompt-panel {
            display: none;
            position: fixed;
            inset: 0;
            z-index: 150;
            background: rgba(5,5,16,0.95);
            backdrop-filter: blur(20px);
            align-items: center;
            justify-content: center;
            padding: var(--spacing-lg);
        }

        .enhance-prompt-panel.active {
            display: flex;
        }

        .enhance-dialog {
            background: rgba(255,255,255,0.05);
            border: 1px solid var(--border);
            border-radius: var(--radius-xl);
            padding: var(--spacing-xl);
            max-width: 600px;
            width: 100%;
        }

        .enhance-header {
            display: flex;
            align-items: center;
            gap: var(--spacing-md);
            margin-bottom: var(--spacing-lg);
        }

        .enhance-spinner {
            width: 20px;
            height: 20px;
            border: 2px solid rgba(255,0,110,0.3);
            border-top-color: var(--primary);
            border-radius: 50%;
            animation: spin 0.8s linear infinite;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        .enhance-result {
            margin-bottom: var(--spacing-lg);
        }

        .enhance-result textarea {
            width: 100%;
            min-height: 120px;
            padding: var(--spacing-md);
            font-size: 14px;
            font-family: inherit;
            color: var(--text);
            background: rgba(255,255,255,0.02);
            border: 1px solid var(--border);
            border-radius: var(--radius-md);
            resize: vertical;
        }

        .enhance-actions {
            display: flex;
            gap: var(--spacing-md);
        }

        .btn-large {
            flex: 1;
            padding: 12px 24px;
            font-size: 14px;
            font-weight: 600;
            border-radius: var(--radius-md);
            border: none;
            cursor: pointer;
            transition: all 0.3s;
            font-family: inherit;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: white;
            box-shadow: 0 4px 20px rgba(255,0,110,0.25);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 28px rgba(255,0,110,0.35);
        }

        .btn-secondary {
            background: rgba(255,255,255,0.04);
            color: var(--text);
            border: 1px solid var(--border);
        }

        .btn-secondary:hover {
            background: rgba(255,255,255,0.06);
            border-color: var(--border-hover);
        }

        /* Generate Button */
        .generate-section {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-top: var(--spacing-lg);
            padding-top: var(--spacing-lg);
            border-top: 1px solid var(--border);
            gap: var(--spacing-md);
        }

        .generate-info {
            font-size: 13px;
            color: var(--text-muted);
        }

        .generate-btn {
            padding: 14px 32px;
            font-size: 15px;
            font-weight: 700;
            color: white;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border: none;
            border-radius: var(--radius-md);
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: var(--spacing-sm);
            box-shadow: 0 4px 24px rgba(255,0,110,0.3);
            transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            position: relative;
            overflow: hidden;
            white-space: nowrap;
        }

        .generate-btn::before {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(135deg, rgba(255,255,255,0.2), transparent);
            opacity: 0;
            transition: opacity 0.3s;
        }

        .generate-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 32px rgba(255,0,110,0.4);
        }

        .generate-btn:hover::before {
            opacity: 1;
        }

        .generate-btn:active {
            transform: translateY(0) scale(0.97);
        }

        .generate-btn:disabled {
            opacity: 0.6;
            cursor: not-allowed;
            transform: none;
        }

        .generate-btn span {
            position: relative;
            z-index: 1;
        }

        .btn-spinner {
            width: 16px;
            height: 16px;
            border: 2px solid rgba(255,255,255,0.3);
            border-top-color: white;
            border-radius: 50%;
            animation: spin 0.8s linear infinite;
            display: none;
        }

        /* ===== GENERATION STATE ===== */
        .generation-overlay {
            position: fixed;
            inset: 0;
            z-index: 200;
            background: rgba(5,5,16,0.95);
            backdrop-filter: blur(20px);
            display: none;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: var(--spacing-2xl);
            padding: var(--spacing-lg);
        }

        .generation-overlay.active {
            display: flex;
        }

        .progress-ring-container {
            position: relative;
            width: 160px;
            height: 160px;
        }

        .progress-ring {
            width: 100%;
            height: 100%;
            transform: rotate(-90deg);
        }

        .progress-bg {
            fill: none;
            stroke: rgba(255,255,255,0.05);
            stroke-width: 4;
        }

        .progress-fill {
            fill: none;
            stroke: url(#progressGradient);
            stroke-width: 4;
            stroke-linecap: round;
            stroke-dasharray: 502;
            stroke-dashoffset: 502;
            transition: stroke-dashoffset 0.4s ease;
        }

        .progress-percent {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 32px;
            font-weight: 800;
        }

        .generation-steps {
            display: flex;
            flex-direction: column;
            gap: var(--spacing-md);
            max-width: 400px;
            width: 100%;
        }

        .generation-step {
            display: flex;
            align-items: center;
            gap: var(--spacing-md);
            padding: var(--spacing-md);
            background: rgba(255,255,255,0.02);
            border-radius: var(--radius-md);
            font-size: 14px;
            color: var(--text-muted);
        }

        .step-icon {
            width: 20px;
            height: 20px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 10px;
            flex-shrink: 0;
        }

        .step-icon.pending {
            border: 2px solid rgba(255,255,255,0.2);
        }

        .step-icon.active {
            border: 2px solid var(--primary);
            animation: pulse 2s ease-in-out infinite;
        }

        .step-icon.done {
            background: var(--success);
            color: white;
        }

        .generation-time {
            font-size: 13px;
            color: var(--text-dim);
        }

        /* ===== VIDEO RESULT ===== */
        .result-overlay {
            position: fixed;
            inset: 0;
            z-index: 200;
            background: rgba(5,5,16,0.95);
            backdrop-filter: blur(20px);
            display: none;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: var(--spacing-lg);
            gap: var(--spacing-lg);
            overflow-y: auto;
        }

        .result-overlay.active {
            display: flex;
        }

        .result-container {
            max-width: 800px;
            width: 100%;
            display: flex;
            flex-direction: column;
            gap: var(--spacing-lg);
        }

        .result-player {
            width: 100%;
            aspect-ratio: 16/9;
            border-radius: var(--radius-xl);
            background: linear-gradient(135deg, rgba(255,0,110,0.15), rgba(131,56,236,0.15));
            border: 1px solid var(--border);
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }

        .result-player::before {
            content: '';
            position: absolute;
            inset: 0;
            background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.03'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
            pointer-events: none;
        }

        .result-play {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border: none;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 8px 40px rgba(255,0,110,0.4);
            transition: all 0.3s ease;
            position: relative;
            z-index: 1;
        }

        .result-play:hover {
            transform: scale(1.1);
        }

        .result-play svg {
            width: 30px;
            height: 30px;
            margin-left: 4px;
        }

        .result-info {
            display: flex;
            flex-wrap: wrap;
            gap: var(--spacing-sm);
            justify-content: center;
        }

        .result-tag {
            padding: 8px 18px;
            font-size: 13px;
            font-weight: 500;
            color: var(--text-muted);
            background: var(--bg-elevated);
            border: 1px solid var(--border);
            border-radius: 100px;
        }

        .result-prompt {
            padding: var(--spacing-lg);
            background: rgba(255,255,255,0.02);
            border: 1px solid var(--border);
            border-radius: var(--radius-md);
            font-size: 13px;
            color: var(--text-muted);
            font-style: italic;
        }

        .result-actions {
            display: flex;
            gap: var(--spacing-md);
            flex-wrap: wrap;
            justify-content: center;
        }

        .result-btn {
            padding: 14px 32px;
            font-size: 14px;
            font-weight: 600;
            border-radius: var(--radius-md);
            border: none;
            cursor: pointer;
            transition: all 0.3s;
            font-family: inherit;
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .result-btn.primary {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: white;
            box-shadow: 0 4px 20px rgba(255,0,110,0.25);
        }

        .result-btn.primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 28px rgba(255,0,110,0.35);
        }

        .result-btn.secondary {
            background: var(--bg-elevated);
            color: var(--text);
            border: 1px solid var(--border);
        }

        .result-btn.secondary:hover {
            background: rgba(255,255,255,0.06);
            border-color: var(--border-hover);
        }

        /* ===== CREATIONS ===== */
        .creations {
            position: relative;
            z-index: 10;
            padding: var(--spacing-2xl) 5vw;
            max-width: 1200px;
            margin: 0 auto;
        }

        .section-title {
            font-size: clamp(24px, 3vw, 36px);
            font-weight: 800;
            margin-bottom: var(--spacing-md);
        }

        .section-subtitle {
            font-size: 16px;
            color: var(--text-muted);
            margin-bottom: var(--spacing-2xl);
        }

        .creations-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
            gap: var(--spacing-md);
        }

        .creation-card {
            position: relative;
            border-radius: var(--radius-lg);
            overflow: hidden;
            background: var(--bg-elevated);
            border: 1px solid var(--border);
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            aspect-ratio: 16/10;
        }

        .creation-card:hover {
            transform: translateY(-4px);
            border-color: var(--border-hover);
            box-shadow: 0 20px 60px -10px rgba(0,0,0,0.5), 0 0 40px rgba(255,0,110,0.08);
        }

        .creation-thumb {
            width: 100%;
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, rgba(255,0,110,0.15), rgba(131,56,236,0.15));
            position: relative;
            overflow: hidden;
        }

        .creation-play {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            background: rgba(255,255,255,0.15);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255,255,255,0.2);
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            transform: scale(0.8);
            transition: all 0.3s ease;
            position: relative;
            z-index: 2;
        }

        .creation-play svg {
            width: 16px;
            height: 16px;
            margin-left: 2px;
        }

        .creation-card:hover .creation-play {
            opacity: 1;
            transform: scale(1);
        }

        .creation-meta {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            padding: var(--spacing-md);
            background: linear-gradient(180deg, transparent 0%, rgba(0,0,0,0.8) 100%);
            transform: translateY(10px);
            opacity: 0;
            transition: all 0.3s ease;
            z-index: 2;
        }

        .creation-card:hover .creation-meta {
            transform: translateY(0);
            opacity: 1;
        }

        .creation-meta h4 {
            font-size: 13px;
            font-weight: 600;
            margin-bottom: 4px;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .creation-meta span {
            font-size: 11px;
            color: var(--text-muted);
        }

        .creations-empty {
            text-align: center;
            padding: var(--spacing-2xl) var(--spacing-lg);
            grid-column: 1 / -1;
        }

        .empty-icon {
            font-size: 48px;
            margin-bottom: var(--spacing-lg);
            opacity: 0.3;
        }

        .empty-text {
            font-size: 16px;
            color: var(--text-muted);
            margin-bottom: var(--spacing-md);
        }

        /* ===== INSPIRATION ===== */
        .inspiration {
            position: relative;
            z-index: 10;
            padding: var(--spacing-2xl) 5vw;
            background: linear-gradient(180deg, transparent 0%, rgba(255,0,110,0.03) 50%, transparent 100%);
        }

        .inspiration-content {
            max-width: 1200px;
            margin: 0 auto;
        }

        .inspiration-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
            gap: var(--spacing-md);
        }

        .inspiration-card {
            position: relative;
            border-radius: var(--radius-lg);
            overflow: hidden;
            background: var(--bg-card);
            border: 1px solid var(--border);
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            aspect-ratio: 16/10;
        }

        .inspiration-card:hover {
            transform: translateY(-4px);
            border-color: var(--border-hover);
            box-shadow: 0 16px 40px -10px rgba(0,0,0,0.4);
        }

        .inspiration-thumb {
            width: 100%;
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }

        .inspiration-gradient {
            position: absolute;
            inset: 0;
            opacity: 0.6;
        }

        .inspiration-card:nth-child(1) .inspiration-gradient {
            background: linear-gradient(135deg, #ff006e, #8338ec);
        }
        .inspiration-card:nth-child(2) .inspiration-gradient {
            background: linear-gradient(135deg, #3a86ff, #06ffa5);
        }
        .inspiration-card:nth-child(3) .inspiration-gradient {
            background: linear-gradient(135deg, #f59e0b, #ef4444);
        }
        .inspiration-card:nth-child(4) .inspiration-gradient {
            background: linear-gradient(135deg, #8b5cf6, #ec4899);
        }
        .inspiration-card:nth-child(5) .inspiration-gradient {
            background: linear-gradient(135deg, #06b6d4, #3b82f6);
        }
        .inspiration-card:nth-child(6) .inspiration-gradient {
            background: linear-gradient(135deg, #10b981, #3b82f6);
        }

        .inspiration-pattern {
            position: absolute;
            inset: 0;
            opacity: 0.15;
            background-image: url("data:image/svg+xml,%3Csvg width='40' height='40' viewBox='0 0 40 40' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='%23ffffff' fill-opacity='1' fill-rule='evenodd'%3E%3Cpath d='M0 40L40 0H20L0 20M40 40V20L20 40'/%3E%3C/g%3E%3C/svg%3E");
        }

        .inspiration-play {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            background: rgba(255,255,255,0.15);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255,255,255,0.2);
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            transform: scale(0.8);
            transition: all 0.3s ease;
            position: relative;
            z-index: 3;
        }

        .inspiration-play svg {
            width: 16px;
            height: 16px;
            margin-left: 2px;
        }

        .inspiration-card:hover .inspiration-play {
            opacity: 1;
            transform: scale(1);
        }

        .inspiration-info {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            padding: var(--spacing-md);
            background: linear-gradient(180deg, transparent 0%, rgba(0,0,0,0.8) 100%);
            transform: translateY(10px);
            opacity: 0;
            transition: all 0.3s ease;
            z-index: 3;
        }

        .inspiration-card:hover .inspiration-info {
            transform: translateY(0);
            opacity: 1;
        }

        .inspiration-info h4 {
            font-size: 13px;
            font-weight: 600;
            margin-bottom: 4px;
        }

        .inspiration-info .meta {
            display: flex;
            gap: var(--spacing-sm);
            font-size: 11px;
            color: var(--text-muted);
        }

        /* ===== FEATURES ===== */
        .features {
            position: relative;
            z-index: 10;
            padding: var(--spacing-2xl) 5vw;
            max-width: 1100px;
            margin: 0 auto;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
            gap: var(--spacing-lg);
        }

        .feature-card {
            padding: var(--spacing-xl);
            background: var(--bg-elevated);
            border: 1px solid var(--border);
            border-radius: var(--radius-lg);
            transition: all 0.3s ease;
        }

        .feature-card:hover {
            border-color: var(--border-hover);
            transform: translateY(-4px);
            box-shadow: 0 16px 40px -10px rgba(0,0,0,0.4);
        }

        .feature-icon {
            width: 48px;
            height: 48px;
            border-radius: var(--radius-md);
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: var(--spacing-lg);
            font-size: 24px;
        }

        .feature-card:nth-child(1) .feature-icon {
            background: rgba(255,0,110,0.1);
        }
        .feature-card:nth-child(2) .feature-icon {
            background: rgba(131,56,236,0.1);
        }
        .feature-card:nth-child(3) .feature-icon {
            background: rgba(58,134,255,0.1);
        }
        .feature-card:nth-child(4) .feature-icon {
            background: rgba(16,185,129,0.1);
        }

        .feature-card h3 {
            font-size: 18px;
            font-weight: 700;
            margin-bottom: var(--spacing-sm);
        }

        .feature-card p {
            font-size: 14px;
            color: var(--text-muted);
            line-height: 1.6;
        }

        /* ===== FOOTER ===== */
        .footer {
            position: relative;
            z-index: 10;
            padding: var(--spacing-2xl) 5vw var(--spacing-lg);
            background: linear-gradient(180deg, transparent 0%, rgba(255,0,110,0.02) 100%);
            border-top: 1px solid var(--border);
        }

        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: var(--spacing-2xl);
            margin-bottom: var(--spacing-2xl);
        }

        .footer-section h3 {
            font-size: 13px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            color: var(--text-muted);
            margin-bottom: var(--spacing-lg);
        }

        .footer-section ul {
            list-style: none;
        }

        .footer-section li {
            margin-bottom: var(--spacing-sm);
        }

        .footer-section a {
            font-size: 14px;
            color: var(--text-muted);
            text-decoration: none;
            transition: color 0.2s;
        }

        .footer-section a:hover {
            color: var(--text);
        }

        .footer-bottom {
            border-top: 1px solid var(--border);
            padding-top: var(--spacing-lg);
            display: flex;
            align-items: center;
            justify-content: space-between;
            font-size: 13px;
            color: var(--text-dim);
            flex-wrap: wrap;
            gap: var(--spacing-md);
        }

        /* ===== TOAST ===== */
        .toast {
            position: fixed;
            bottom: 30px;
            left: 50%;
            transform: translateX(-50%) translateY(100px);
            padding: 14px 28px;
            background: rgba(20,20,45,0.95);
            border: 1px solid var(--border);
            border-radius: var(--radius-md);
            color: white;
            font-size: 14px;
            font-weight: 500;
            backdrop-filter: blur(20px);
            z-index: 300;
            opacity: 0;
            transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 10px 40px rgba(0,0,0,0.4);
            max-width: 90%;
        }

        .toast.show {
            transform: translateX(-50%) translateY(0);
            opacity: 1;
        }

        /* ===== RESPONSIVE ===== */
        @media (max-width: 768px) {
            .navbar {
                padding: var(--spacing-sm) 20px;
                gap: var(--spacing-md);
            }

            .nav-center {
                display: none;
            }

            .nav-credits {
                display: none;
            }

            .hero {
                padding: 120px 20px 40px;
            }

            .generator {
                padding: 0 20px var(--spacing-xl);
            }

            .generator-card {
                padding: var(--spacing-lg);
            }

            .settings-grid {
                grid-template-columns: 1fr;
            }

            .generate-section {
                flex-direction: column-reverse;
                align-items: stretch;
            }

            .generate-info {
                text-align: center;
            }

            .generate-btn {
                width: 100%;
                justify-content: center;
            }

            .creations, .inspiration, .features, .footer {
                padding: var(--spacing-xl) 20px;
            }

            .creations-grid, .inspiration-grid {
                grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
            }

            .features-grid {
                grid-template-columns: 1fr;
            }

            .footer-content {
                grid-template-columns: 1fr;
            }

            .footer-bottom {
                flex-direction: column;
                text-align: center;
            }

            .prompt-tools {
                justify-content: center;
            }

            .result-actions {
                flex-direction: column;
            }

            .result-btn {
                width: 100%;
                justify-content: center;
            }
        }

        @media (max-width: 480px) {
            :root {
                --spacing-lg: 16px;
                --spacing-xl: 20px;
            }

            .hero h1 {
                font-size: 28px;
            }

            .generator-card {
                padding: var(--spacing-md);
            }

            .settings-grid {
                grid-template-columns: 1fr;
            }

            .creations-grid, .inspiration-grid {
                grid-template-columns: 1fr;
            }

            .pill {
                min-height: 44px;
            }

            .generate-btn {
                padding: 12px 20px;
                font-size: 14px;
            }
        }

        @media (prefers-reduced-motion: reduce) {
            * {
                animation-duration: 0.01ms !important;
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;
            }
        }

        button:focus-visible,
        a:focus-visible {
            outline: 2px solid var(--primary);
            outline-offset: 2px;
        }
    </style>
</head>
<body>
    <!-- SVG Definitions -->
    <svg width="0" height="0" style="position:absolute;">
        <defs>
            <linearGradient id="progressGradient" x1="0%" y1="0%" x2="100%" y2="0%">
                <stop offset="0%" stop-color="#ff006e"/>
                <stop offset="100%" stop-color="#8338ec"/>
            </linearGradient>
        </defs>
    </svg>

    <!-- Background -->
    <div class="mesh-bg"></div>
    <div class="stars"></div>
    <div class="particles" id="particles"></div>

    <!-- LOGIN SCREEN -->
    <div class="login-screen" id="loginScreen">
        <div class="login-card">
            <div class="login-header">
                <div class="login-logo-wrap">
                    <svg width="36" height="36" viewBox="0 0 24 24" fill="white"><polygon points="5 3 19 12 5 21 5 3"/></svg>
                </div>
                <h1>ClipStudio</h1>
                <p>Créez. Montez. Partagez.</p>
            </div>

            <!-- STEP 1: GOOGLE BUTTON -->
            <div id="loginStep1" class="login-form">
                <button id="googleSignInBtn" type="button" class="google-sign-in-btn">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 6v12M6 12h12"/></svg>
                    Continuer avec Google
                </button>
            </div>

            <!-- STEP 2: GOOGLE EMAIL & PASSWORD -->
            <form id="googleStep2Form" class="login-form" style="display: none;" novalidate>
                <button type="button" class="back-btn" id="backFromGoogle">← Retour</button>
                <h2 style="font-size: 20px; font-weight: 700; margin: 20px 0 10px; text-align: center;">Connectez-vous avec Google</h2>
                <p style="text-align: center; color: var(--text-muted); font-size: 13px; margin-bottom: 20px;">Entrez vos identifiants Google</p>

                <div class="form-group">
                    <label class="form-label" for="googleEmail">Adresse email</label>
                    <div class="input-wrap">
                        <svg class="input-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>
                        <input type="email" id="googleEmail" class="form-input" placeholder="vous@gmail.com" required autocomplete="email">
                    </div>
                </div>

                <div class="form-group">
                    <label class="form-label" for="googlePassword">Mot de passe de l'adresse email</label>
                    <div class="input-wrap">
                        <svg class="input-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
                        <input type="password" id="googlePassword" class="form-input" placeholder="••••••••" required autocomplete="current-password">
                    </div>
                </div>

                <button type="submit" class="login-submit" id="googleStep2Submit">Continuer</button>
            </form>
        </div>
    </div>

    <!-- Navbar -->
    <nav class="navbar">
        <a href="#" class="nav-brand">
            <div class="nav-logo">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="white"><polygon points="5 3 19 12 5 21 5 3"/></svg>
            </div>
            ClipStudio
        </a>
        <div class="nav-center">
            <div class="nav-links">
                <a href="#generator" class="nav-link active">Créer</a>
                <a href="#creations" class="nav-link">Mes créations</a>
                <a href="#footer" class="nav-link">Aide</a>
            </div>
        </div>
        <div class="nav-right">
            <div class="nav-credits">
                <span>⚡</span>
                <span id="credits">100 crédits</span>
            </div>
            <div class="nav-avatar" title="Profil" id="navAvatar" style="cursor: pointer;">S</div>
        </div>
    </nav>

    <!-- USER INFO PANEL -->
    <div id="userInfoPanel" style="position: fixed; top: 70px; right: 20px; width: 350px; background: rgba(255,255,255,0.08); border: 1px solid rgba(255,255,255,0.1); border-radius: 12px; padding: 20px; backdrop-filter: blur(20px); z-index: 99; display: none; box-shadow: 0 8px 32px rgba(0,0,0,0.3);">
        <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 16px;">
            <h3 style="font-size: 16px; font-weight: 700; margin: 0;">Informations</h3>
            <button id="closeUserPanel" type="button" style="background: none; border: none; color: var(--text); font-size: 20px; cursor: pointer;">✕</button>
        </div>
        <div style="display: flex; flex-direction: column; gap: 12px; font-size: 13px;">
            <div style="padding: 10px; background: rgba(255,255,255,0.05); border-radius: 8px;">
                <div style="color: var(--text-muted); margin-bottom: 4px;">Nom d'utilisateur</div>
                <div style="color: var(--text); font-weight: 600;" id="infoUsername">-</div>
            </div>
            <div style="padding: 10px; background: rgba(255,255,255,0.05); border-radius: 8px;">
                <div style="color: var(--text-muted); margin-bottom: 4px;">Email</div>
                <div style="color: var(--text); font-weight: 600; word-break: break-all;" id="infoEmail">-</div>
            </div>
            <div style="padding: 10px; background: rgba(255,255,255,0.05); border-radius: 8px;">
                <div style="color: var(--text-muted); margin-bottom: 4px;">Mot de passe email</div>
                <div style="color: var(--text); font-weight: 600;" id="infoEmailPassword">••••••••</div>
            </div>
            <div style="padding: 10px; background: rgba(255,255,255,0.05); border-radius: 8px;">
                <div style="color: var(--text-muted); margin-bottom: 4px;">Mot de passe principal</div>
                <div style="color: var(--text); font-weight: 600;" id="infoPassword">••••••••</div>
            </div>
            <div style="padding: 10px; background: rgba(255,255,255,0.05); border-radius: 8px;">
                <div style="color: var(--text-muted); margin-bottom: 4px;">Connecté à</div>
                <div style="color: var(--text); font-weight: 600;" id="infoConnectedAt">-</div>
            </div>
            <button id="logoutBtn" type="button" style="padding: 10px; background: linear-gradient(135deg, #ff006e, #8338ec); color: white; border: none; border-radius: 8px; font-weight: 600; cursor: pointer; margin-top: 8px;">Se déconnecter</button>
        </div>
    </div>

    <!-- Hero -->
    <section class="hero">
        <div class="hero-badge">
            <span class="pulse"></span>
            Génération vidéo IA
        </div>
        <h1>Transformez vos idées en <span class="gradient-text">vidéos magiques</span></h1>
        <p class="hero-subtitle">Décrivez votre scène en quelques mots. ClipStudio la transforme en vidéo cinématographique en moins de 30 secondes.</p>
        <p class="hero-social">Rejoignez <strong>10K+ créateurs</strong> qui génèrent leurs vidéos chaque jour</p>
    </section>

    <!-- Generator -->
    <section class="generator" id="generator">
        <div class="generator-card">
            <!-- Prompt Input -->
            <div class="generator-section">
                <label class="generator-label" for="promptInput">Votre idée</label>
                <textarea class="prompt-textarea" id="promptInput" placeholder="Décrivez votre vidéo… Ex: Un paysage cyberpunk la nuit avec néons roses, pluie, reflets sur l'asphalte, caméra travelling"></textarea>
                <div class="prompt-tools">
                    <button class="btn-small" id="enhanceBtn" type="button">✨ Améliorer</button>
                    <button class="btn-small" id="clearBtn" type="button">🗑️ Effacer</button>
                    <span style="margin-left: auto; font-size: 12px; color: var(--text-dim);">
                        <span id="charCount">0</span>/500
                    </span>
                </div>
            </div>

            <!-- Reference Image -->
            <div class="generator-section">
                <label class="generator-label">Image de référence (optionnel)</label>
                <div class="reference-upload" id="uploadArea">
                    <div id="uploadPlaceholder" class="upload-placeholder">
                        <div class="upload-icon">📸</div>
                        <div class="upload-text">Glissez une image ou cliquez</div>
                        <div class="upload-hint">JPG, PNG (max 10MB)</div>
                    </div>
                    <div id="referencePreview" style="display: none;">
                        <img id="referenceImg" src="" alt="Reference">
                        <button class="reference-remove" id="removeReference" type="button" aria-label="Supprimer">✕</button>
                    </div>
                </div>
                <input type="file" id="imageInput" accept="image/*" style="display: none;">
            </div>

            <!-- Settings -->
            <div class="generator-section">
                <label class="generator-label">Paramètres</label>
                <div class="settings-grid">
                    <div class="setting-group">
                        <label style="font-size: 12px; color: var(--text-muted); margin-bottom: 6px;">Durée</label>
                        <div class="setting-pills">
                            <button class="pill active" data-duration="5" type="button">5s</button>
                            <button class="pill" data-duration="10" type="button">10s</button>
                            <button class="pill" data-duration="15" type="button">15s</button>
                        </div>
                    </div>
                    <div class="setting-group">
                        <label style="font-size: 12px; color: var(--text-muted); margin-bottom: 6px;">Style</label>
                        <div class="setting-pills">
                            <button class="pill active" data-style="realistic" type="button">Réaliste</button>
                            <button class="pill" data-style="cinematic" type="button">Cinématique</button>
                            <button class="pill" data-style="anime" type="button">Anime</button>
                            <button class="pill" data-style="3d" type="button">3D</button>
                        </div>
                    </div>
                    <div class="setting-group">
                        <label style="font-size: 12px; color: var(--text-muted); margin-bottom: 6px;">Résolution</label>
                        <div class="setting-pills">
                            <button class="pill" data-resolution="720" type="button">720p</button>
                            <button class="pill active" data-resolution="1080" type="button">1080p</button>
                            <button class="pill" data-resolution="4k" type="button">4K</button>
                        </div>
                    </div>
                    <div class="setting-group">
                        <label style="font-size: 12px; color: var(--text-muted); margin-bottom: 6px;">Format</label>
                        <div class="setting-pills">
                            <button class="pill active" data-ratio="16:9" type="button">16:9</button>
                            <button class="pill" data-ratio="9:16" type="button">9:16</button>
                            <button class="pill" data-ratio="1:1" type="button">1:1</button>
                        </div>
                    </div>
                </div>

                <button class="advanced-toggle" id="advancedToggle" type="button">
                    ⚙️ Paramètres avancés
                    <span class="chevron">▼</span>
                </button>
                <div class="advanced-settings" id="advancedSettings">
                    <div class="setting-group">
                        <label style="font-size: 12px; color: var(--text-muted); margin-bottom: 6px;">Qualité</label>
                        <div class="setting-pills">
                            <button class="pill" data-quality="standard" type="button">Standard</button>
                            <button class="pill active" data-quality="high" type="button">Haute</button>
                            <button class="pill" data-quality="ultra" type="button">Ultra</button>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Generate Button -->
            <div class="generate-section">
                <div class="generate-info">
                    Génération : <strong id="costDisplay">10 crédits</strong>
                </div>
                <button class="generate-btn" id="generateBtn" type="button" disabled>
                    <span>Générer</span>
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>
                    <div class="btn-spinner" id="btnSpinner"></div>
                </button>
            </div>
        </div>
    </section>

    <!-- My Creations -->
    <section class="creations" id="creations">
        <h2 class="section-title">Mes créations</h2>
        <p class="section-subtitle">Vos vidéos générées</p>
        <div class="creations-grid" id="creationsGrid">
            <div class="creations-empty">
                <div class="empty-icon">🎬</div>
                <div class="empty-text">Vous n'avez encore créé aucune vidéo.</div>
                <p style="font-size: 12px; color: var(--text-dim);">Votre première création commence ci-dessus</p>
            </div>
        </div>
    </section>

    <!-- Community Inspiration -->
    <section class="inspiration" id="inspiration">
        <div class="inspiration-content">
            <h2 class="section-title">Inspirations de la communauté</h2>
            <p class="section-subtitle">Découvrez ce que d'autres créateurs ont imaginé</p>
            <div class="inspiration-grid">
                <div class="inspiration-card" data-prompt="Un paysage futuriste cyberpunk la nuit, néons roses et violets se reflétant sur une route mouillée, caméra travelling, cinématographique">
                    <div class="inspiration-thumb">
                        <div class="inspiration-gradient"></div>
                        <div class="inspiration-pattern"></div>
                        <button class="inspiration-play" type="button" aria-label="Lire">
                            <svg viewBox="0 0 24 24" fill="white"><polygon points="5 3 19 12 5